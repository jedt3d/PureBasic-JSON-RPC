# 031 Security Robustness

This scenario checks core robustness boundaries: trace payload defaults,
malformed-message recovery, and write failure recovery.

Build through the committed project file:

```sh
./tools/build.sh
```

Run the probe:

```sh
./.build/examples/031-security-robustness/security_probe
```
