# 008 Batch Handling Scenario

This scenario demonstrates sequential JSON-RPC batch dispatch.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/008-batch-handling/batch_probe
```

Expected output:

```text
batch handling scenario: OK
```
