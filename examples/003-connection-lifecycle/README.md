# 003 Connection Lifecycle Scenario

This scenario demonstrates connection startup, fake writer capture, and idempotent shutdown.

Run:

```sh
./tools/build.sh
./.build/examples/003-connection-lifecycle/connection_probe
```

Expected output:

```text
connection scenario: OK
```

