# 015 MCP Tools Call Scenario

This scenario demonstrates MCP `tools/call`.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/015-mcp-tools-call/mcp_tools_call_probe
```

Expected output:

```text
mcp tools call scenario: OK
```
