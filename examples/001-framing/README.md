# 001 Framing Scenario

This scenario demonstrates the first JSON-RPC library slice: `Content-Length` framed message writing and incremental frame reading.

Run:

```sh
./tools/build.sh
./.build/examples/001-framing/framing_probe
```

Expected output:

```text
framing scenario: OK
```

