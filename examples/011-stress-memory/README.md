# 011 Stress And Memory Scenario

This scenario runs a bounded parse, dispatch, timeout, and cancellation stress smoke.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/011-stress-memory/stress_probe
```

Expected output:

```text
stress memory scenario: OK
```
