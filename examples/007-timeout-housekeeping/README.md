# 007 Timeout Housekeeping Scenario

This scenario demonstrates pending outbound request timeout cleanup.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/007-timeout-housekeeping/timeout_probe
```

Expected output:

```text
timeout housekeeping scenario: OK
```
