# 006 Outbound Requests Scenario

This scenario demonstrates outbound JSON-RPC request ids, notification sends, and pending response matching.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/006-outbound-requests/outbound_probe
```

Expected output:

```text
outbound requests scenario: OK
```
