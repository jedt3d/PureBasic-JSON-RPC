# 009 Cancellation Scenario

This scenario demonstrates cooperative `$/cancelRequest` handling.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/009-cancellation/cancel_probe
```

Expected output:

```text
cancellation scenario: OK
```
