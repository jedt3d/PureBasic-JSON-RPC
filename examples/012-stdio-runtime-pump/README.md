# 012 Stdio Runtime Pump Scenario

This scenario demonstrates feeding newline-delimited stdio messages into the runtime pump.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/012-stdio-runtime-pump/stdio_runtime_probe
```

Expected output:

```text
stdio runtime scenario: OK
```
