# 014 MCP Tools Registry Scenario

This scenario demonstrates MCP tool registration and `tools/list`.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/014-mcp-tools-registry/mcp_tools_list_probe
```

Expected output:

```text
mcp tools registry scenario: OK
```
