# 016 Packaging And Docs Scenario

This scenario verifies the consolidated include and compile templates.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/016-packaging-docs/package_probe
```

Expected output:

```text
packaging docs scenario: OK
```
