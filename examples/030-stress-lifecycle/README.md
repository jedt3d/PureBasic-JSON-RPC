# 030 Stress Lifecycle

This scenario runs the reusable lifecycle stress helper for the JSON-RPC
connection stack.

Build through the committed project file:

```sh
./tools/build.sh
```

Run the probe:

```sh
./.build/examples/030-stress-lifecycle/stress_lifecycle_probe
```
