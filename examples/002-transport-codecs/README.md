# 002 Transport Codecs Scenario

This scenario demonstrates MCP-compatible stdio message handling: one UTF-8 JSON-RPC message per line.

Run:

```sh
./tools/build.sh
./.build/examples/002-transport-codecs/stdio_codec_probe
```

Expected output:

```text
stdio codec scenario: OK
```

