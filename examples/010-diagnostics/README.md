# 010 Diagnostics Scenario

This scenario demonstrates connection diagnostics counters.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/010-diagnostics/diagnostics_probe
```

Expected output:

```text
diagnostics scenario: OK
```
