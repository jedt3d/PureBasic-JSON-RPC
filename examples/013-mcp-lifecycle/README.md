# 013 MCP Lifecycle Scenario

This scenario demonstrates the MCP `initialize` request and `notifications/initialized` notification.

Build with:

```sh
./tools/build.sh
```

Run with:

```sh
./.build/examples/013-mcp-lifecycle/mcp_lifecycle_probe
```

Expected output:

```text
mcp lifecycle scenario: OK
```
